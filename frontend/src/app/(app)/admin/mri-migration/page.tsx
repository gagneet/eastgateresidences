import type {Metadata} from "next";

export const metadata: Metadata = {
    title: "MRI Migration | StrataOS",
    description: "Import and migrate data from MRI strata management system",
};
/**
 * @generated FunctionHeader
 * Function: MriMigrationPage
 * Path: frontend/src/app/(app)/admin/mri-migration/page.tsx
 *
 * @remarks Generated inventory header. Replace or expand this with reviewed business-purpose documentation before relying on it as source commentary.
 */
export default function MriMigrationPage() {
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const MriMigrationPageComponent = require("@/pages/dashboard/MriMigrationPage").default;
    return <MriMigrationPageComponent/>;
}
